{
  "name": "cse340-schmitz-practice",
  "dependencies": {
    "ejs": "^4.0.1",
    "express": "^5.2.1",
    "pg": "^8.18.0"
  },
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file=.env server.js ",
    "dev": "nodemon --env-file=.env server.js"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "ws": "^8.19.0"
  }
}
